<%= form_with(model: superintendence, local: true) do |form| %>
  <% if superintendence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(superintendence.errors.count, "error") %> prohibited this superintendence from being saved:</h2>

      <ul>
      <% superintendence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field" style="display: none">
    <%= form.label :management_id %>
    <%= form.number_field :management_id, :value => @management.id %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
